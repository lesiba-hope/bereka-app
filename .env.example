# =============================================================================
# Bereka Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values.
# For Supabase Edge Function secrets, use: supabase secrets set KEY=VALUE
# For Vercel, set these in the Vercel dashboard under Settings > Environment Variables.
# =============================================================================

# ---------------------
# Frontend (Next.js)
# ---------------------
# Set these in apps/web/.env.local (or Vercel dashboard for production)
# ⚠️ IMPORTANT: Use EXACTLY this variable name (not PUBLISHABLE_DEFAULT_KEY or any other variation)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# ---------------------
# Supabase Edge Function Secrets
# ---------------------
# Set these via: supabase secrets set KEY=VALUE
# They are automatically available to all Edge Functions via Deno.env.get()

# Supabase (auto-set in hosted, needed for local dev)
# SUPABASE_URL=https://your-project-ref.supabase.co
# SUPABASE_ANON_KEY=your-anon-key
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# LNbits
LNBITS_URL=https://your-lnbits-instance.com
LNBITS_ADMIN_KEY=your-lnbits-admin-key
LNBITS_WEBHOOK_SECRET=your-webhook-secret

# Email (optional — if not set, notifications are logged to console)
# Email (Resend)
RESEND_API_KEY=re_123456789

# ---------------------
# GitHub Actions Secrets
# ---------------------
# Set these in GitHub repo Settings > Secrets and Variables > Actions
# SUPABASE_ACCESS_TOKEN=your-supabase-access-token
# SUPABASE_DB_PASSWORD=your-db-password
# SUPABASE_PROJECT_ID=your-project-ref
